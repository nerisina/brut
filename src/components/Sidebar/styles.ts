import styled from 'styled-components';

export const SidebarContainer = styled.div`
display: flex;
flex-direction: column;
    img{
        width: 400px;
        position: fixed;
        bottom: 0;
    }`;